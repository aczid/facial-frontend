dependency "merb-action-args", "1.0.4"   # Provides support for querystring arguments to be passed in to controller actions
dependency "merb-assets", "1.0.4"        # Provides link_to, asset_path, auto_link, image_tag methods (and lots more)
dependency "merb-cache", "1.0.4"         # Provides your application with caching functions 
dependency "merb-helpers", "1.0.4"       # Provides the form, date/time, and other helpers
dependency "merb-mailer", "1.0.4"        # Integrates mail support via Merb Mailer
dependency "merb-slices", "1.0.4"        # Provides a mechanism for letting plugins provide controllers, views, etc. to your app
dependency "merb-auth", "1.0.4"          # An authentication slice (Merb's equivalent to Rails' restful authentication)
dependency "merb-param-protection", "1.0.4"
 
dependency "dm-core", "0.9.8"         # The datamapper ORM
dependency "dm-aggregates", "0.9.8"   # Provides your DM models with count, sum, avg, min, max, etc.
dependency "dm-migrations", "0.9.8"   # Make incremental changes to your database.
dependency "dm-timestamps", "0.9.8"   # Automatically populate created_at, created_on, etc. when those properties are present.
dependency "dm-types", "0.9.8"        # Provides additional types, including csv, json, yaml.
dependency "dm-validations", "0.9.8"  # Validation framework
dependency "dm-serializer", "0.9.8"  # Serialization framework
